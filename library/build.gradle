apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'
group = 'com.github.zhengdj'
android {
    compileSdkVersion 26
    buildToolsVersion "26.0.3"

    defaultConfig {
        minSdkVersion 5
        targetSdkVersion 26

    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
